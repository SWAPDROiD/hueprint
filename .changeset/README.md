# Changesets

This folder tracks release notes and version updates for `hueprint`.

- Run `npx changeset` after any user-facing change.
- Commit the generated markdown file in `.changeset/`.
- Push to `main` to let GitHub Actions open or update the release PR.
- Merging that PR will publish the package to npm.
